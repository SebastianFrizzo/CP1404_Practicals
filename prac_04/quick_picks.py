""" Asks user for number of 'quick picks' and then generates that many lines of output."""
import random


def main():
	number_of_quick_picks = int(input("How many quick picks?: "))
	for :
		output_list.append(range(1, 46))